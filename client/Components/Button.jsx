import React from 'react';
import styles from './Button.css';

const Button = (props) => {
  return (
    <button className="the_button" type="submit" form="form">
      Check availability / Reserve
    </button>
  );
};

export default Button;
