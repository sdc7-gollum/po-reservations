import React from 'react';
import ReactDOM from 'react-dom';

const Reservation = () => (
  <h2>This is a rendering test.</h2>
);

ReactDOM.render(<Reservation />, document.getElementById('reservation'));

export default Reservation;
